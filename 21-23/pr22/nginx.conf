http {
	upstream backend {
  	server app1:3000 max_fails=2 fail_timeout=30s; # Сервер 1
  	server app2:3001 max_fails=2 fail_timeout=30s; # Сервер 2
  	server app3:3002 backup; # Резервный сервер
  }

  server {
  	listen 80;
  	location / {
  		proxy_pass http://backend;
  	}
  }
}

events {}